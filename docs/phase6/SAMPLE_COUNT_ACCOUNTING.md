# Sample-count accounting

The proof ledger exports expansion-key rows, compaction-key rows, same-entry row-difference samples, and deployment-dependent input ciphertext rows. These counts must be used by every parameter and security claim.
